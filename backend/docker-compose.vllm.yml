
################################################################################
# vLLM Service - Qwen 2.5 7B AWQ (production model)
# Model: Qwen/Qwen2.5-7B-Instruct-AWQ (7B params, AWQ quantized)
# GPU: RTX 3060 12GB - fits with AWQ quantization
# Port: 8002 (OpenAI-compatible API)
################################################################################

services:
  vllm:
    image: vllm/vllm-openai:latest
    container_name: mangwale_vllm
    restart: unless-stopped
    runtime: nvidia
    ports:
      - "8002:8000"
    environment:
      - HF_TOKEN=${HF_TOKEN:-}
      - NVIDIA_VISIBLE_DEVICES=0
      - CUDA_VISIBLE_DEVICES=0
      - VLLM_ATTENTION_BACKEND=FLASHINFER
    volumes:
      - ${HOME}/.cache/huggingface:/root/.cache/huggingface
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    command: >
      --model ${VLLM_MODEL:-Qwen/Qwen2.5-7B-Instruct-AWQ}
      --port 8000
      --host 0.0.0.0
      --max-model-len 4096
      --tensor-parallel-size 1
      --gpu-memory-utilization 0.85
      --dtype auto
      --max-num-seqs 8
      --enforce-eager
      --disable-log-requests
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
